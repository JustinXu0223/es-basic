/**
 * 用途
 * 1. 函数参数的定义
 */

// 参数是一组有次序的值
const f1 = ([x, y, z]) => {
  console.log(`${x};${y};${z}`);
};
f1([1, 2, 3]); // 1;2;3

// 参数是一组无次序的值
const f2 = ({ x, y, z }) => {
  console.log(`${x};${y};${z}`);
};
f2({ z: 3, y: 2, x: 1 }); // 1;2;3
